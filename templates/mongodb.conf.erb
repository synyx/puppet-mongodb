#
# DO NOT EDIT - THIS FILE IS MANAGED VIA PUPPET
#

# mongodb.conf

# Where to store the data.

# Note: if you run mongodb as a non-root user (recommended) you may
# need to create and set permissions for this directory manually,
# e.g., if the parent directory isn't mutable by the mongodb user.
dbpath = <%= dbpath_real %>

#where to log
logpath = <%= logpath_real %>

logappend = <%= logappend_real %>

port = <%= port_real%>

# Disables write-ahead journaling
nojournal = <%= nojournal_real %>

# Enables periodic logging of CPU utilization and I/O wait
cpu = <%= cpu_real %>

# Turn on/off security. Off is currently the default
#noauth = true
auth = <%= auth_real %>

# Verbose logging output.
verbose = <%= verbose_real %>

# Inspect all client data for validity on receipt (useful for
# developing drivers)
objcheck = <%= objcheck_real %>

# Enable db quota management
quota = <%= quota_real %>

# Set oplogging level where n is
#   0=off (default)
#   1=W
#   2=R
#   3=both
#   7=W+some reads
diaglog = <%= diaglog_real %>

# Ignore query hints
#nohints = true

# Disable the HTTP interface (Defaults to localhost:28017).
nohttpinterface = <%= nohttpinterface_real %>

# Turns off server-side scripting.  This will result in greatly limited
# functionality
noscripting = <%= noscripting_real %>

# Turns off table scans.  Any query that would do a table scan fails.
notablescan = <%= notablescan_real %>

# Disable data file preallocation.
noprealloc = <%= noprealloc_real %>

# Specify .ns file size for new databases.
nssize = <%= nssize_real %>

<% if @mms_token_real %>
# Accout token for Mongo monitoring server.
mms-token = <%= @mms_token_real %>
<% end %>

<% if @mms_name_real %>
# Server name for Mongo monitoring server.
mms-name = <%= @mms_name_real %>
<% end %>

<% if @mms_interval_real %>
# Ping interval for Mongo monitoring server.
mms-interval = <%= @mms_interval_real %>
<% end %>

# Replication Options

# in master/slave replicated mongo databases, specify here whether
# this is a slave or master
<% if @slave_real %>
slave = <%= @slave_real %>
<% end %>

<% if @master_real %>
master = <%= @master_real %>
<% end %>

<% if @source_real %>
source = <%= @source_real %>
<% end %>

<% if @only_real %>
# Slave only: specify a single database to replicate
only = <%= @only_real %>
<% end %>

<% if @replSet_real %>
# in replica set configuration, specify the name of the replica set
replSet = <%= @replSet_real %>
<% end %>
